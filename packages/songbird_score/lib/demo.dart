/// Scores generated on demand, for trying the library out on something larger
/// than a fixture will comfortably hold.
///
/// A full orchestral score is megabytes of MusicXML and awkward to keep in a
/// repository; generating one costs a moment and can be asked for any length,
/// which is what makes it useful both for a benchmark and for seeing how the
/// widgets feel with a symphony in them.
library;

export 'src/demo/orchestral_score.dart';
export 'src/demo/showcase_score.dart';
